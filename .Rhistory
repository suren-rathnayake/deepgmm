mat <- matrix(rnorm(10000, 100)^2, nrow=10000, ncol=100)
means <- apply(mat, 1, mean)
length(means)
X2 <- rowSums(mat)
h = hist(X, col="red")
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
mat <- matrix(rnorm(10000, 100)^2, nrow=10000, ncol=100)
means <- apply(mat, 1, mean)
length(means)
X <- rowSums(mat)
h = hist(X, col="red")
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
mat <- matrix(rnorm(10000, 100)^2, nrow=10000, ncol=100)
means <- apply(mat, 1, mean)
vars <- apply(mat, 1, vars)
X <- rowSums(mat)
h = hist(X, col="red")/vars
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
mat <- matrix(rnorm(10000, 100)^2, nrow=10000, ncol=100)
means <- apply(mat, 1, mean)
vars <- apply(mat, 1, var)
X <- rowSums(mat)
h = hist(X, col="red")/vars
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
vars <- apply(mat, 1, var)
vars
mat <- matrix(rnorm(10000, 100)^2, nrow=10000, ncol=100)
mat[1,]
mat[2,]
mat <- matrix(rnorm(10000*100)^2, nrow=10000, ncol=100)
means <- apply(mat, 1, mean)
vars <- apply(mat, 1, var)
X <- rowSums(mat)
h = hist(X, col="red")/vars
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
mat <- matrix(rnorm(10000*100)^2, nrow=10000, ncol=100)
means <- apply(mat, 1, mean)
vars <- apply(mat, 1, var)
X <- rowSums(mat)/vars
h = hist(X, col="red")
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
mat <- matrix(rnorm(10000*200)^2, nrow=10000, ncol=200)
means <- apply(mat, 1, mean)
vars <- apply(mat, 1, var)
X <- rowSums(mat)/vars
h = hist(X, col="red")
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
mat <- matrix(rnorm(10000*200)^2, nrow=10000, ncol=100)
means <- apply(mat, 1, mean)
X <- rowSums(mat)
h = hist(X, col="red")
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
mat <- matrix(runif(10000*200)^2, nrow=10000, ncol=100)
means <- apply(mat, 1, mean)
X <- rowSums(mat)
h = hist(X, col="red")
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
mat <- matrix(runif(10000*200)^2, nrow=10000, ncol=200)
means <- apply(mat, 1, mean)
X <- rowSums(mat)
h = hist(X, col="red")
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
mat <- matrix(runif(10000*200)^2, nrow=10000, ncol=200)
means <- apply(mat, 1, mean)
X <- rowSums(mat)
h = hist(X, col="red")
xfit <- seq(min(X), max(X), length(10000))
yfit <- dnorm(xfit, mean(X), sd(X))
yfit <- yfit*diff(h$mids[1:2])*length(X)
lines(xfit, yfit, col="blue", lwd=2)
max(X)
summary(X)
var(X)
mean(X)
hist(means)
means <- apply(mat, 1, mean)
vars <- apply(mat, 1, vars)
vars <- apply(mat, 1, var)
hist(vars)
hist(vars, breaks=20)
ls
pwd
ls()
install.packages("~/2018/package_release/deep_gmm_pkg/deepgmm_0.1.36.tar.gz", repos = NULL, type = "source")
setwd("~/2018/package_release/deep_gmm_pkg/deepgmm")
install.packages("kableExtra")
install.packages("scales")
